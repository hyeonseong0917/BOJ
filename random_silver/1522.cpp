#include <iostream>
#include <algorithm>
#include <vector>
#include <cstring>
using namespace std;

#define ll long long



int main() {
	ios_base :: sync_with_stdio(false); 
	cin.tie(NULL); 
	cout.tie(NULL);
	string s;
	int ans=0;
	cin>>s;	
	// a를 모두 연속하게 바꾸는 데 필요한 최소 횟수
	// 1. b 사이의 거리가 가장 먼 구간을 찾는다 X
	// 2. 
	return 0;
}